Compiling the code:
--Navigate to firstName_lastName_assign1/Starting-with-java/src and enter the following command to compile
javac *.java

Executing the code
--In the same folder type in java Driver amazon.wsdl which gives the required output.

Time and Space Complexity
--We are storing n key value pairs into the hash map the space complexity id O(n)
--Retrieving the value from the hash map is constant time. So overall time complexity is O(1) or linear.